

    //  else if (i < 1000) {
    //   const ones = i % 10
    //   const tens = parseInt(i / 10) % 10
    //   const hundreds = parseInt(i / 100)

let number = 1
while (number <= 10000) {
  let notSelfNumbers = []
  // num = 222
  const ones = num % 10
  const tens = parseInt(num / 10) % 10
  const hundreds = parsInt(num / 100)
  const thousands = parseInt(num / 1000)
}